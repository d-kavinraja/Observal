name: DCO Check

on:
  pull_request:
    branches: [main]

jobs:
  dco:
    runs-on: ubuntu-latest
    steps:
      - name: DCO Check
        uses: christophebedard/dco-check@0.5.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
